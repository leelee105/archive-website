# Archive — Shared file storage

Browse, upload, create folders, rename, move, and delete files. No login required.

## Run the server

```bash
npm install
npm start
```

Open http://localhost:3000

## Data

- Metadata (folders and file list) is stored in `data/data.json`.
- Uploaded file contents are stored in `data/uploads/`.
